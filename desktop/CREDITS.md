# Credits

Some attribution here is required by licenses; most is here because we like
giving credit.

- **Tauri 2** — the native shell, bundler, and updater plumbing for macOS,
  Windows, and Linux.
- **VibeXStudio** (this project's sibling repo) — the entire frontend is its
  Expo web export; see its CREDITS.md for the app-level stack.
- **Media Lab** — the optional sidecar server; see its CREDITS.md for the
  engines and models it orchestrates.
- **Rust** and the crates Tauri pulls in — the reason these binaries are a
  few megabytes instead of a few hundred.
- **GitHub Actions** — builds the Windows and Linux releases; our own fleet
  builds and notarizes macOS.

If you see your work here uncredited, open an issue — we'll fix it.
